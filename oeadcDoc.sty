\ProvidesPackage{oeadcDoc}

\usepackage{fancyhdr} % For fancy footer.
\usepackage{lastpage} % For page numbering.

\usepackage{thaiSupport} % Load common confiuration for Thai.
\usepackage{dottedField} % For dotted field.

% fancyhdr setup.
% Clear the lines of the header and footer.
\renewcommand{\footrulewidth}{0pt}
\renewcommand{\headrulewidth}{0pt}
